## Helionyx Environment Configuration (Example)
#
# This file intentionally matches the set of configuration variables that the
# code currently reads (see shared/common/config.py).
#
# Recommended workflow:
# - Use .env as a NON-SECRET base file (shared defaults)
# - Put secrets + env-specific overrides in .env.dev / .env.staging / .env.live
#
# For node1 systemd deployments, the unit loads .env.{env}.
#
# Copy this file to an environment-specific file and fill in real values:
#   cp .env.example .env.dev
#   cp .env.example .env.staging
#   cp .env.example .env.live

# =============================================================================
# Storage Configuration
# =============================================================================

EVENT_STORE_PATH=./data/events
PROJECTIONS_DB_PATH=./data/projections/helionyx.db

# Backup/restore scripts (optional override)
BACKUP_ROOT=

# =============================================================================
# API Server Configuration
# =============================================================================

API_HOST=0.0.0.0
API_PORT=8000

# =============================================================================
# OpenAI LLM Configuration
# =============================================================================

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.7

OPENAI_RATE_LIMIT_RPM=10
OPENAI_RATE_LIMIT_TPM=150000

LLM_MAX_RETRIES=3
LLM_RETRY_BASE_DELAY=1.0

LLM_DAILY_COST_WARNING_USD=1.0
LLM_DAILY_COST_LIMIT_USD=10.0

# =============================================================================
# Telegram Bot Configuration
# =============================================================================

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

NOTIFICATIONS_ENABLED=true
DAILY_SUMMARY_HOUR=20
REMINDER_WINDOW_START=8
REMINDER_WINDOW_END=21
REMINDER_ADVANCE_HOURS=24

# =============================================================================
# Logging
# =============================================================================

LOG_LEVEL=INFO


