# Helionyx Development Environment Configuration
#
# This is a complete example for the DEVELOPMENT environment.
# 
# TO USE:
#   1. Copy this file: cp .env.dev.example .env.dev
#   2. Replace <placeholder> values with your actual credentials
#   3. Run with: ENV=dev make run (or specify ENV=dev in your command)
#
# ENVIRONMENT: Development (verbose logging, low cost limits, local paths)
# PORT: 8000 (standard dev port)
# SERVICE: helionyx-dev.service

# =============================================================================
# Environment
# =============================================================================
# Note: ENV is set via CLI (ENV=dev), not in this file

# =============================================================================
# API Server
# =============================================================================
# Bind to all interfaces for local network access
API_HOST=0.0.0.0

# Dev environment uses port 8000
API_PORT=8000

# =============================================================================
# Storage (Local Development Paths)
# =============================================================================
# Event store - relative path for easy access and testing
EVENT_STORE_PATH=./data/dev/events

# Projections database - separate from staging/live
PROJECTIONS_DB_PATH=./data/dev/projections/helionyx.db

# Backups (optional). If you use an encrypted mount on node1, point this there.
BACKUP_ROOT=/var/lib/helionyx/backups

# =============================================================================
# OpenAI LLM
# =============================================================================
# Required: Your OpenAI API key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=<your_openai_api_key_here>

# Use cost-efficient model for development
OPENAI_MODEL=gpt-4o-mini

# Standard parameters
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.7

# Rate limiting (generous for dev experimentation)
OPENAI_RATE_LIMIT_RPM=10
OPENAI_RATE_LIMIT_TPM=150000

# Error handling
LLM_MAX_RETRIES=3
LLM_RETRY_BASE_DELAY=1.0

# Cost control - LOW limits for development to prevent runaway costs
LLM_DAILY_COST_WARNING_USD=0.5
LLM_DAILY_COST_LIMIT_USD=2.0

# =============================================================================
# Telegram Bot (Development Bot)
# =============================================================================
# Required: Create a SEPARATE bot for dev via @BotFather
# Bot name suggestion: helionyx_dev_bot
#
# Setup:
#   1. Open Telegram, chat with @BotFather
#   2. Send: /newbot
#   3. Name: helionyx_dev_bot  (or similar)
#   4. Copy the token below
#
TELEGRAM_BOT_TOKEN=<your_dev_bot_token_from_botfather>

# Your Telegram user chat ID
# Get by: Start your dev bot, send /start, check logs for chat ID
TELEGRAM_CHAT_ID=<your_telegram_user_chat_id>

# Notification settings - DISABLED for dev to avoid spam during testing
NOTIFICATIONS_ENABLED=false

# If you enable notifications for dev testing:
DAILY_SUMMARY_HOUR=20
REMINDER_WINDOW_START=8
REMINDER_WINDOW_END=21
REMINDER_ADVANCE_HOURS=24

# =============================================================================
# Logging
# =============================================================================
# Verbose logging for development debugging
LOG_LEVEL=DEBUG
